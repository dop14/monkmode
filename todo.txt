Save data if closeEVENT -> only if elapsed_time >= 60 seconds
Save data if timer is stopped -> only if elapsed_time >= 60 seconds

# Count elapsed_time -> TIME SPENT IN FOCUS, WITHOUT BREAKS
# Create timer logic (focus, break, long break)

#Play sound at 2 sec
Create schema for focus goal
    - daily focus goal default: 1 hour
    - weekly focus goal default: (weekdays || whole week) * daily focus goal

Default: weekdays only